// Una SPA (Single Page Application) es una aplicación web que carga una sola página HTML al inicio y, a partir de ahí, actualiza el contenido dinámicamente con JavaScript sin recargar toda la página.

// Puntos clave:

// El navegador no “navega” a páginas nuevas completas; solo cambia partes del contenido.
// Las rutas suelen manejarse con JavaScript (por ejemplo, usando el History API).
// Ofrece una experiencia más fluida, parecida a una app de escritorio.
// Requiere más lógica en el front-end y suele comunicarse con APIs para obtener datos.
// Si quieres, puedo agregar ejem